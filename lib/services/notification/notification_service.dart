/// Notifications push (FCM) + déclencheurs Edge Functions — squelette.
class NotificationService {
  NotificationService._();

  static Future<void> initialize() async {
    // TODO: brancher Firebase Messaging + handlers Supabase
  }
}
