# Back-office admin MadBeauty — site statique dans web/
# Variables Netlify (dashboard ou CLI) : SUPABASE_URL, SUPABASE_ANON_KEY

[build]
  command = "node scripts/generate_web_admin_config.js"
  publish = "web"

[build.environment]
  NODE_VERSION = "22"
