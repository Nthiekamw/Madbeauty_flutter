// Identifiant App Store / production (Release).
APPLICATION_BUNDLE_ID = com.nthiekamw.madbeauty

// Nom App Store Connect : MadBeauty (voir CoreStrings.appStoreListingName).
// Nom sous l’icône : aligné sur le listing (peut être tronqué sur petits écrans).
